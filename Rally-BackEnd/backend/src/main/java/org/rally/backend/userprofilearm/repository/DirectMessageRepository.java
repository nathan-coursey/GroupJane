package org.rally.backend.userprofilearm.repository;

import org.rally.backend.userprofilearm.model.DirectMessage;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.Optional;

public interface DirectMessageRepository {
    // extends JpaRepository<DirectMessage, Integer>
//    @Override
//    Optional<DirectMessage> findById(Integer integer);
}

export class EventCategory {

    id: number;
    eventCategory: string;

    constructor(id: number, eventCategory: string) {
        this.id = id;
        this.eventCategory = eventCategory;
    }
}